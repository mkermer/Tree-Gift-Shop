import { render, screen } from '@testing-library/react';
import App from './App';

test('our first tree test case - please edit me soon', () => {
  render(<App />);
  // const linkElement = screen.getByText(/learn react/i);
  // expect(linkElement).toBeInTheDocument();
});
